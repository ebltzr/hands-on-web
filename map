    # using map()
    my_list = [1, 2, 3] 
    map(lambda x: f'#{x}', my_list) # ['#1', '#2', '#3']
    
    # using join()
    ', '.join(my_list) # "1, 2, 3"
    
    # combining map() and join()
    ', '.join(map(lambda x: f"#{x+1}", my_list)) # "#1, #2, #3"